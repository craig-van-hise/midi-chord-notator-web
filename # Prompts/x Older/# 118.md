# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The core pipeline is stable. We are now implementing precise UX refinements for MIDI Learn mappings, Virtual Toggle Mode behavior, and Hold Mode audio sustain. **Do not modify the overarching architecture; execute these changes surgically.**

## 2. Implementation Phases

### Phase 1: MIDI Learn Sequence & Learn Interceptor Update — ✅ Completed
* **Objective:** Reorder the learn sequence to group DOWN actions before UP actions, and ensure the global interceptor correctly identifies `isNoteOff` to block it during Learn mode.
* **Instructions:**
  1. Open `src/components/toolbar/TransformationsContainer.tsx` (or `Drawer`).
  2. Locate the `INITIAL_BUTTONS` array (or the sequence passed to `startLearnMode`). Change it to: 
     `['SEMI_DOWN', 'SEMI_UP', 'KEY_DOWN', 'KEY_UP', 'ROT_DOWN', 'ROT_UP', 'OCT_DOWN', 'OCT_UP', 'UNDO', 'REDO', 'PLAY', 'HOME']`
  3. In the global `window.__MIDI_INTERCEPTOR` block, ensure `isNoteOff` is explicitly checked and silenced during Learn Mode:
     ```typescript
     const isNoteOff = (status & 0xF0) === 0x80 || ((status & 0xF0) === 0x90 && velocity === 0);
     if (learnState.isActive) {
       if (isNoteOn) {
         const currentId = learnState.sequence[learnState.currentButtonIndex];
         updateButtonConfig(currentId, { midiNote: note });
         if (learnState.currentButtonIndex >= learnState.sequence.length - 1) {
           setLearnState(prev => ({ ...prev, isActive: false }));
         } else {
           setLearnState(prev => ({ ...prev, currentButtonIndex: prev.currentButtonIndex + 1 }));
         }
       }
       return true; // Unconditionally block both ON and OFF during learn
     }
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Execute changes. Set status to ✅ Completed.

### Phase 2: Toggle & Hold Mode Audio Isolation — ✅ Completed
* **Objective:** Fix the "Radio Button" toggle bug by ignoring virtual NoteOffs, enforce a 500ms 1/4-note duration for Toggle audio, and ensure physical Hold Mode sends audio `releaseNote` reliably.
* **Instructions:**
  1. Open `src/components/NotationCanvas.tsx`. Locate `handleMidiMessage`.
  2. Inside the `isNoteOn` block, in the VIRTUAL TOGGLE section:
     Change the audio trigger to a timed 500ms release (1/4 note at 120BPM):
     ```typescript
     audioEngine.noteOn(Tone.Frequency(note, "midi").toNote(), velocity / 127);
     setTimeout(() => audioEngine.releaseNote(Tone.Frequency(note, "midi").toNote()), 500);
     ```
  3. Inside the `isNoteOff` block, completely bypass Virtual Toggle events:
     ```typescript
     else if (isNoteOff) {
       if (isVirtual && isToggleModeActive) {
         return; // DO NOTHING. Visual toggle was handled on click down, audio has a timeout.
       }
       
       // PHYSICAL OR MOMENTARY MODE
       physicalKeysDown.current.delete(note);
       audioEngine.releaseNote(Tone.Frequency(note, "midi").toNote());

       if (!isHoldModeActive) {
         activeNotes.current = activeNotes.current.filter(n => n.note !== note);
         updateSpellings();
       }
       
       if (isHoldModeActive && physicalKeysDown.current.size === 0) {
         isWaitingForNewChord.current = true;
       }
       
       updateActiveNotes?.([...activeNotes.current]);
     }
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Execute changes. Set status to ✅ Completed.

### Phase 3: Hardware Playback Envelopes (Note Length & Velocity) — ✅ Completed
* **Objective:** Mapped hardware keys must sustain audio as long as the key is physically held, using the hardware's exact velocity, completely replacing the hardcoded `setTimeout` preview.
* **Instructions:**
  1. Open `src/components/NotationCanvas.tsx`. Locate the `APP_TRANSFORM` event listener. **Remove** the block of code at the bottom that automatically plays audio (e.g., calls `playPreviewNotes(transposedStrings, true)`). The transformation should now be silent by default.
  2. Create two new CustomEvent listeners inside the `useEffect`:
     ```typescript
     const handlePreviewOn = (e: Event) => {
         const vel = (e as CustomEvent).detail.velocity / 127;
         const strings = Array.from(selectedNoteIds.current)
           .map(id => activeNotes.current.find(n => n.id === id)?.note)
           .filter((n): n is number => typeof n === 'number')
           .map(pitch => Tone.Frequency(pitch, "midi").toNote());
         strings.forEach(s => audioEngine.noteOn(s, vel));
     };
     const handlePreviewOff = () => {
         const strings = Array.from(selectedNoteIds.current)
           .map(id => activeNotes.current.find(n => n.id === id)?.note)
           .filter((n): n is number => typeof n === 'number')
           .map(pitch => Tone.Frequency(pitch, "midi").toNote());
         strings.forEach(s => audioEngine.releaseNote(s));
     };
     window.addEventListener('APP_HARDWARE_PREVIEW_ON', handlePreviewOn);
     window.addEventListener('APP_HARDWARE_PREVIEW_OFF', handlePreviewOff);
     // Ensure you remove these in the cleanup return
     ```
  3. Open `src/components/toolbar/TransformationsDrawer.tsx`. Update the `isNoteOn` / `isNoteOff` mapping logic in the global interceptor:
     ```typescript
     // Inside window.__MIDI_INTERCEPTOR
     const match = Object.keys(configs).find(id => configs[id as ButtonId].midiNote === note && note !== -1);
     if (match) {
       if (isNoteOn) {
         handleButtonDown(match as ButtonId); // Updates math/canvas visually
         window.dispatchEvent(new CustomEvent('APP_HARDWARE_PREVIEW_ON', { detail: { velocity } }));
       } else if (isNoteOff) {
         handleButtonUp(match as ButtonId); // Visually releases button
         window.dispatchEvent(new CustomEvent('APP_HARDWARE_PREVIEW_OFF'));
       }
       return true; // Consume note
     }
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Execute changes. Set status to ✅ Completed.

## 3. Final Review
* Verify all 3 sub-phases are marked ✅ Completed.
* Request the user to manually verify:
  1. Toggle Mode properly turns keys on/off with a 500ms audio ping.
  2. Hold Mode drops audio the moment the physical key is lifted.
  3. Mapped MIDI Learn keys sustain chords exactly as long as they are held, reacting to initial velocity.