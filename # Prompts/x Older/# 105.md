# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
**Objective:** Resolve a UI glitch where the closed ROMPler drawer is "peeking" into the viewport, and execute a global branding update.
**Current State:** The Framer Motion translation math for the closed ROMPler footer is incorrect, leaving the top pixels of the main drawer visible. The app is currently titled "MIDI Chord Notator".
**End State:** The ROMPler drawer will be perfectly flush with the bottom of the screen when closed, showing *only* the toggle tab. The application will be globally renamed to "VV | MIDI Chord Notator" in both the DOM title and the UI header.

## 2. Technical Decisions & Dependencies
* **Framer Motion Drawer Math:** The previous `calc(100% - 32px)` instruction caused the peek because it assumed the tab was part of the element's total layout height. The worker agent MUST change the closed animation state strictly to `y: "100%"` and ensure the toggle tab is positioned with `absolute bottom-full left-0` (or `-top-X` matching its exact height) so it protrudes *outside* the bounding box of the moving container.
* **Branding Locations:** The rebrand requires touching two distinct layers: the static `index.html` (for the browser tab) and the top-level Header/Nav component (for the UI). 

## 3. Implementation Phases

### Phase 1: Fix Footer Peeking Bug — 🔘 Pending
* **Objective:** Eliminate the visible sliver of the ROMPler drawer when it is in the closed state.
* **Instructions:**
  1. Open `src/components/RomplerFooter.tsx`.
  2. Locate the Framer Motion `animate` property on the main wrapper.
  3. Change the closed state value: `animate={{ y: isOpen ? 0 : "100%" }}`.
  4. Locate the DOM element for the toggle tab. Ensure its positioning explicitly pushes it above the main container's top edge (e.g., `absolute bottom-full left-0` or `absolute -top-10 left-0`).
* **TDD Checkpoint:**
    * **Test Case 1:** [Given `<RomplerFooter isOpen={false} />`], [Assert the Framer Motion style is `transform: translateY(100%)`].
    * **Test Case 2:** [Given the Toggle Tab element], [Assert it possesses positioning classes that place it strictly outside the top boundary of its parent wrapper].
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute Framer Motion and CSS fixes. Once passing, set status to ✅ Completed. Do not proceed until status is ✅."

### Phase 2: Global Rebrand — 🔘 Pending
* **Objective:** Update the application name to "VV | MIDI Chord Notator" in the document head and UI.
* **Instructions:**
  1. Open `index.html` at the project root.
  2. Update the `<title>` tag to exactly: `<title>VV | MIDI Chord Notator</title>`.
  3. Open the top-level layout or header component (e.g., `App.tsx` or `Header.tsx`) where the UI title is rendered.
  4. Update the text string. Maintain the existing typography split (e.g., standard text for "VV | MIDI Chord" and colored/purple text for "Notator").
* **TDD Checkpoint:**
    * **Test Case 1:** [Given `index.html`], [Assert `<title>` strictly equals `VV | MIDI Chord Notator`].
    * **Test Case 2:** [Given the Header component], [Assert it renders the text "VV | MIDI Chord Notator"].
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply the text replacements. Once validated, set status to ✅ Completed."
    * **HUMAN VALIDATION REQUIRED:** "STOP. The agent cannot visually verify the drawer peek bug is resolved. Ask the user to: 1) Verify the browser tab name is correct. 2) Look at the bottom of the screen and confirm ONLY the Rompler tab is visible when closed, with zero pixels of the dark drawer peeking out."

## 4. Final Review & Validation
* **Technical Audit:** Verify that setting `y: "100%"` did not introduce a vertical scrollbar on the `<body>` element. If it did, ensure `overflow-hidden` is present on the outermost absolute/fixed wrapper containing the Framer Motion element.
* **Status Reconciliation:** Verify all Phase markers are marked ✅ Completed. Ensure there are no 🚨 Error markers remaining.
* **Final Human Sign-off:** Present the applied changes to the user for final commit.