# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
The previous attempt to implement 3-note cluster cohemitonia (the `xLevel` greedy algorithm) caused a severe regression. It destroyed meticulously hand-crafted, functioning collision detection and offset math for accidentals and standard 2nd intervals. 

You are strictly forbidden from writing new accidental layout algorithms from scratch. Your sole objective is to use Git to retrieve the exact, previously working mathematical logic from the repository history, restore it, and surgically integrate the new 3-note cluster horizontal spacing *around* the original, pristine math.

## 2. Technical Decisions & Dependencies
**Strict Git Restoration Protocol:**
1. The agent must locate the commit immediately preceding the introduction of the "Greedy X-Level Assignment" algorithm.
2. The agent must extract the exact file contents of `src/utils/notationMath.ts` (and any related rendering components) from that specific commit.
3. The agent must isolate the specific functions/loops responsible for accidental left-sweeping and 2nd-interval notehead offsets.
4. The agent must integrate the restored math as the immutable baseline, only applying the new `xLevel >= 2` horizontal padding logic when a 3rd contiguous semitone is detected.

## 3. Implementation Phases

### Phase 1: Git Retrieval & Logic Extraction — 🔘 Pending
* **Objective:** Interrogate the local Git repository to extract the precise, pre-regression rendering mathematics.
* **Instructions:**
    1. Execute `git log --oneline` in the terminal to identify the commit hash prior to the cluster notation/xLevel feature branch or commits.
    2. Execute `git show <COMMIT_HASH>:src/utils/notationMath.ts` (and any other relevant rendering files).
    3. Read and analyze the output to explicitly locate the exact variables, padding constants, and collision detection loops previously authored by the user for accidental offsets and 2nd intervals.
    4. Copy this exact logic into your working memory. Do not alter its core mathematical constants.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the extracted logic is restored to the current branch, When the test suite runs a standard C to Db 2nd interval, Assert the exact previous X-offset values are produced].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the Git commands, restore the logic, and write/run the verification test. Once it PASSES, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Surgical Integration of Cohemitonia Spacing — 🔘 Pending
* **Objective:** Introduce the necessary `xLevel >= 2` spacing for 3+ note clusters without touching the restored baseline logic for levels 0 and 1.
* **Instructions:**
    1. Implement the `xLevel` clustering logic strictly for identifying when a note is pushed to level 2 or higher.
    2. When calculating the final X-translation in the rendering layer:
        * If `xLevel <= 1`: Use the **exact** math extracted in Phase 1.
        * If `xLevel >= 2`: Calculate the offset by taking the base `xLevel * NOTEHEAD_WIDTH` and adding the width of any attached accidentals, ensuring the 3rd note is pushed far enough right so its accidental does not bleed into the `xLevel 1` space.
    3. Ensure the restored accidental left-sweep algorithm accurately reads the new starting X-positions of the `xLevel >= 2` notes.
* **TDD Checkpoint:**
    * **Test Case 1 (Restored Baseline):** [Given a standard 2nd interval (C, Db), When rendered, Assert the accidental and notehead offsets match the exact mathematical output of the pre-regression codebase].
    * **Test Case 2 (Dense Cluster):** [Given a 3-note cluster (C, C#, D#), When rendered, Assert the D# notehead is translated rightward sufficiently to prevent its sharp from overlapping the C# notehead].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Integrate the logic and execute tests. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅.

## 4. Final Review & Validation
* **Technical Audit:** Verify that no hardcoded layout constants from the original user logic were deleted or modified during the integration.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:** STOP. Present the completed code. Instruct the user to verify the exact UI rendering of a dense cluster and confirm that their original accidental offsets are functioning exactly as they were before the regression. Await user confirmation before marking the top-level PRP status as ✅ Completed.