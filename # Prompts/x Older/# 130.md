

**Context:** Our previous attempt to download the `Bravura.woff2` font resulted in a plain text error message being saved as the font file. We need to download the actual binary file directly from the official repository and verify its integrity before we do anything else.

**Instructions:**

1. Run the following exact command to download the raw binary file directly from Steinberg's official repository:
`curl -sL "https://raw.githubusercontent.com/steinbergmedia/bravura/master/redist/woff2/Bravura.woff2" -o public/fonts/Bravura.woff2`
2. You must now verify that the downloaded file is an actual binary font file and not HTML or plain text bologna. Run this command to check the file type and file size:
`file public/fonts/Bravura.woff2 && ls -lh public/fonts/Bravura.woff2`
3. **Validation Check:** - If the `file` command outputs "HTML document", "ASCII text", or if the file size is tiny (under 10KB), the download failed. Delete the file and tell me it failed.

* If the file is confirmed to be a legitimate binary file (it should say "Web Open Font Format" or similar, and the size should be around 125KB), you have succeeded.

4. Once you confirm it is the correct binary file, stop and wait for my next instruction. **Do not modify any code or configuration files.**