# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
We have successfully implemented the dual-column logic and established the dynamic bounding box for the accidentals, breaking the failure cycle. However, visual inspection reveals that the padding multiplier between the Left Stack's maximum extent and the Right Stack's accidental reach is mathematically too small. 

The `0.2 * staffSpace` padding is resulting in a sub-3-pixel gap, causing the right stack's natural signs to visually "kiss" the left stack's noteheads due to font anti-aliasing and glyph weight.

Your objective is strictly to tune this padding parameter in the layout calculation to introduce standard engraving "breathing room" between the stacks.

## 2. Technical Decisions & Dependencies
**Padding Adjustment:**
The base calculation for the gap currently looks like this:
`rightBaseX = leftMaxX + maxRightAccReachPx + (0.2 * staffSpace);`

You must increase the padding multiplier from `0.2` to `0.8` to provide a clear, undeniable visual boundary between the two clusters.

## 3. Implementation Phases

### Phase 1: Tuning the Inter-Column Padding — ✅ Completed
* **Objective:** Increase the mathematical padding injected into `rightBaseX`.
* **Instructions:**
    1. Open `src/components/NotationCanvas.tsx`.
    2. Locate the line inside the `processGroup` layout function where `rightBaseX` is definitively set.
    3. Modify the padding injection from `0.2 * staffSpace` to `0.8 * staffSpace` (or update the equivalent variable if it was declared separately). 
    4. Do not alter the `leftMaxX` or `maxRightAccReachPx` logic; those are functioning perfectly. Only change the padding constant.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the `C Chromatic5` cluster, When the Right Stack's base is calculated, Assert the resulting `rightBaseX` is exactly `0.6 * staffSpace` larger than the previous layout iteration].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Modify the constant. Once the test PASSES, set status to ✅ Completed. Do not proceed until status is ✅.

## 4. Final Review & Validation
* **Technical Audit:** Verify that no other layout math was touched. This is a surgical single-parameter adjustment.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:** STOP. Present the tuned code. Instruct the user to play the `C Chromatic5` cluster again. Ask the user to visually confirm that there is now a comfortable, readable gap between the left stack's noteheads and the right stack's accidentals. Await user confirmation before marking the top-level PRP status as ✅ Completed.