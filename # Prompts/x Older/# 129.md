
**Context:** We need to replace a broken font file. You must execute this task via the terminal. **CRITICAL INSTRUCTION: You are strictly forbidden from editing, viewing, or modifying any CSS files, HTML files, build configuration files, or any code whatsoever.** **Task:**

1. Delete the file located at `public/fonts/Bravura.woff2`.
2. Run this exact command in the terminal to download the correct binary file:
`curl -L -o public/fonts/Bravura.woff2 https://cdn.jsdelivr.net/npm/@w3c/smufl-bravura@1.3.0/bravura/Bravura.woff2`

**Do absolutely nothing else.** Do not check paths. Do not touch any code. Just delete the file, run the curl command, and then stop and wait for my next instruction.