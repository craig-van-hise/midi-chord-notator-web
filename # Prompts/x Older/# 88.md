# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
During a previous diagnostic phase, a "Wide-Interval Check" was injected into the layout engine to suppress `8va/15ma` shifts for chords spanning both staves, in an attempt to fix a stave-inversion bug. Since the root cause of the stave inversion (an unsorted array in the spelling engine) was successfully resolved in the previous sprint, this wide-interval check is now a regression that permanently disables legitimate Ottava shifts. The objective is to remove this block and restore the independent Treble/Bass Ottava math.

## 2. Technical Decisions & Dependencies
* **Independent Ottava Evaluation:** The `trebleShift` and `bassShift` variables already calculate their thresholds correctly (e.g., `maxTrebleStep >= 21`). They must be allowed to pass directly to `processGroup` without being overridden by a total chord span check.
* **No Other Layout Changes:** Do not modify the `processGroup` math, the zippering logic, or the label rendering.

## 3. Implementation Phases

### Phase 1: Remove the Ottava Kill-Switch — ✅ Completed
* **Objective:** Delete the code block that artificially zeroes out the Ottava shifts.
* **Instructions:**
    1. Open `src/components/NotationCanvas.tsx` and locate the `recalculateLayout` function.
    2. Look for the comment `// Wide-interval check: Disable Ottava shifts if chord spans across both staves`.
    3. Delete that comment and the following block of code entirely:
       ```javascript
       const allPitches = noteDatas.map(n => n.note);
       const totalSpan = Math.max(...allPitches) - Math.min(...allPitches);
       const isWideChord = totalSpan > 24; // 2 octaves

       if (isWideChord) {
         trebleShift = 0;
         trebleLabelText = null;
         bassShift = 0;
         bassLabelText = null;
       }
       ```
    4. Ensure the variables `trebleShift`, `trebleLabelText`, `bassShift`, and `bassLabelText` (calculated just above this block) remain untouched.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Delete the specified code block. Once complete, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify that `trebleShift` and `bassShift` are still declared, calculated, and passed into the `processGroup` function correctly.
* **Status Reconciliation:** Verify Phase 1 is ✅ Completed.
* **Final Human Sign-off:**
    * **HUMAN VALIDATION REQUIRED:** "STOP. I have removed the wide-interval kill-switch. Please test the Ottava logic: place a note in the bass clef, then transpose a treble note high above the staff (above C6 / step 21). Confirm that the 8va/15ma label appears and the notes are correctly shifted down to save space."