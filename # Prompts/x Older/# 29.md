# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
The previous attempt to implement the 3-pass clustering pipeline resulted in a complete data-binding failure. The math utility is no longer successfully passing the calculated horizontal offsets to the React rendering layer, causing all noteheads in a cluster to stack at $X = 0$.

Your primary objective is to debug and restore the data bridge between the layout mathematics and the React rendering component. You must ensure the computed `xOffset` values are actually being applied to the DOM/SVG elements, and then properly implement the level $\ge 2$ dynamic spacing.

## 2. Technical Decisions & Dependencies
**Diagnosis of the Data-Binding Failure:**
1. You likely modified the return signature of the layout utility (e.g., `notationMath.ts`) but failed to update the consuming React component (e.g., `NotationCanvas.tsx`).
2. Alternatively, the React component's inline styling (`style={{ transform: ... }}`) or SVG attributes were accidentally overwritten to ignore the calculated offset property.

**The Fix:**
You must explicitly verify the object structure passed into the `.map()` loop in the rendering layer and ensure the CSS/SVG translation explicitly uses the mathematically calculated offset variable. 

## 3. Implementation Phases

### Phase 1: Restore Data Binding & Baseline Render — 🔘 Pending
* **Objective:** Fix the severed connection between the layout math and the React component.
* **Instructions:**
    1. Inspect the React component responsible for rendering the SMuFL noteheads (e.g., `NotationCanvas.tsx`).
    2. Check the `.map()` function iterating over the notes. Ensure the element's style or transform attribute is explicitly bound to the calculated layout offset (e.g., `transform: translateX(${note.xOffset}px)` or equivalent unit).
    3. Ensure the layout function is actually returning the array of notes with the `xOffset` property intact from the restored baseline logic for levels 0 and 1.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a standard C Major scale played as a cluster (C, D), When the component renders, Assert the React testing DOM shows the 'D' notehead with a non-zero horizontal CSS translation].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Fix the binding and run the UI tests. Once PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Safe Cohemitonia Offset Injection — 🔘 Pending
* **Objective:** Implement the extended rightward shifting for notes at `xLevel >= 2` without breaking the data structure again.
* **Instructions:**
    1. Inside the layout pipeline, immediately after the baseline offsets are calculated for `xLevel <= 1`, implement a safe mutation for the higher levels.
    2. Loop through notes where `xLevel >= 2`.
    3. Calculate their X-coordinate dynamically: $X_{L} = X_{L-1} + W_n + P + (\text{hasAccidental} ? W_a : 0)$, where $W_n$ is notehead width, $W_a$ is accidental width, and $P$ is a small padding constant.
    4. Explicitly assign this computed value to the exact same property key (e.g., `note.xOffset`) that the React component expects.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a 5-note chromatic cluster (C, Db, D, Eb, E), When the layout function executes, Assert the `xOffset` values strictly increase sequentially (e.g., $C < Db < D < Eb < E$) and are not `undefined` or `0`].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute this logic test. Once PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 3: Accidental Rendering Sync — 🔘 Pending
* **Objective:** Ensure accidentals follow their parent noteheads to the new extended X-coordinates.
* **Instructions:**
    1. Verify that the accidental rendering logic in the React component anchors its position relative to the newly bound `note.xOffset`.
    2. Ensure the left-sweeping collision engine receives these updated anchor points so accidentals don't bleed back into lower-level noteheads.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the 5-note chromatic cluster, When rendered, Assert the bounding box or calculated X-position of the 'Eb' accidental is physically to the right of the 'Db' notehead].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Validate the rendering output. Once PASS, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify that no hardcoded `0` values exist in the horizontal translation styles within the React mapping function.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:** STOP. Present the final code. Instruct the user to play the exact "C Chromatic5" chord from their screenshot and visually verify the cascade rightward. Await user confirmation before marking the top-level PRP status as ✅ Completed.