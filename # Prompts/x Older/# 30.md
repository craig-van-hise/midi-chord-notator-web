# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
During the previous phase to fix `xOffset` data binding, a severe regression was introduced: the system stopped respecting enharmonic spellings (Flats vs. Sharps). The application is now naively rendering sharps for all accidentals (e.g., spelling a C Chromatic cluster with C#, D# instead of the theoretically correct Db, Eb supplied by the keyboard/spelling engine).

The worker agent has incorrectly bypassed `src/utils/chordSpeller.ts` (and the `PCS_LUT.json` integration) when passing data into the rendering layer (`NotationCanvas.tsx` or `notationMath.ts`). 

Your sole objective is to restore the data ingestion bridge so that the notation renderer strictly uses the enharmonic spellings provided by the chord spelling engine, without modifying any of the recently fixed horizontal layout (`xOffset`) math.

## 2. Technical Decisions & Dependencies
**Strict Data Source Mandate:**
* The layout engine (`notationMath.ts`) and the rendering component (`NotationCanvas.tsx`) are strictly formatting layers. They are **forbidden** from guessing or recalculating note spellings or accidental types (Sharp, Flat, Natural).
* Every `Note` object mapped in the rendering layer must derive its spelling (and thus its SMuFL accidental glyph) directly from the output of the theoretical spelling engine / state manager. 

## 3. Implementation Phases

### Phase 1: Git Diff & Pipeline Restoration — 🔘 Pending
* **Objective:** Identify exactly where the spelling data was dropped and restore the connection between the parsed chord data and the visual note components.
* **Instructions:**
    1. Execute `git log --oneline` and find the commit hash immediately preceding our work on "cluster notation" and "greedy xLevel algorithms".
    2. Diff `src/components/NotationCanvas.tsx` and the entry point for `notationMath.ts` against that older commit.
    3. Identify how the `Note` objects previously received their `accidental` string/enum (e.g., how the app knew to pass a "b" or "flat" instead of a "#" or "sharp" to the SMuFL rendering switch statement).
    4. Re-integrate this exact spelling ingestion logic into the current codebase. Ensure the layout pipeline maps over these correctly spelled objects, applies the new `xOffset` logic to them, and passes them intact to the React render map.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a simulated MIDI input of `C, Db, D, Eb, E` that triggers the `chordSpeller`, When the data reaches the `NotationCanvas` render function, Assert the Note objects explicitly contain `flat` properties for MIDI 61 and 63, not `sharp`].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the Git diff, restore the pipeline, and run the test. Once it PASSES, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Accidental Glyph Rendering Sync — 🔘 Pending
* **Objective:** Ensure the React rendering layer respects the restored spelling data and outputs the correct SMuFL glyphs.
* **Instructions:**
    1. Verify the code inside `NotationCanvas.tsx` that selects the SMuFL Unicode character (`U+E262` for sharp, `U+E260` for flat, `U+E261` for natural).
    2. Ensure this selection is strictly driven by the `note.accidental` property supplied by the spelling engine, and that no fallback "auto-sharp" logic is overriding it.
    3. Verify that the recent `xOffset` layout math remains fully intact and operational for these newly spelled notes.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the rendering of the `C, Db, D, Eb, E` cluster, When the UI renders, Assert the document contains the SMuFL Flat glyph (`U+E260`) for the Db and Eb notes].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Validate the component rendering. Once PASS, set status to ✅ Completed. Do not proceed until status is ✅.

## 4. Final Review & Validation
* **Technical Audit:** Verify that `chordSpeller.ts` is fully integrated and that `notationMath.ts` is not performing any independent enharmonic conversion.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:** STOP. Present the restored code. Instruct the user to play the exact "C Chromatic5" chord again. Ask them to verify that the staff now correctly displays `C, Db, D natural, Eb, E natural` and that the cascading horizontal offsets are still functioning. Await user confirmation before marking the top-level PRP status as ✅ Completed.