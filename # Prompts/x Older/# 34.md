# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
A critical silent failure has occurred. During the previous execution, the agent reported success, but the UI did not update. Both the right stack noteheads and their accidentals remained in their exact previous positions.

This indicates a catastrophic data-binding or mathematical pipeline failure. The calculated `shiftX` gap is either evaluating to `0`/`NaN`, or the mutated `Note` objects in the `rightStack` are being discarded before reaching the React/imperative rendering layer. 

Your sole objective is to debug the pipeline, trace the exact value of the dynamic shift, ensure the mutated coordinates survive the array merge, and enforce strict binding in the rendering component.

## 2. Technical Decisions & Dependencies
**Diagnostic & Fix Protocol:**
1. **Trace `shiftX`:** You must explicitly calculate and verify the exact numerical value of the gap shift. If it relies on undefined CSS variables or missing SMuFL font metrics at the math-utility level, it will fail silently.
2. **Reference Integrity:** In `notationMath.ts`, when merging `leftStack` and `rightStack` back into a single array, you must ensure you are returning the exact object references that received the `note.xOffset += shiftX` mutation.
3. **Accidental Anchor Binding:** The rendering component (`NotationCanvas.tsx`) MUST calculate the accidental's X-coordinate by explicitly reading `note.xOffset` as its baseline anchor, rather than assuming $X=0$ or a hardcoded left-sweep origin.

## 3. Implementation Phases

### Phase 1: Pipeline Tracing & Math Verification — 🔘 Pending
* **Objective:** Identify why the `RightStack_BaseX` shift calculation resulted in no visual change.
* **Instructions:**
    1. Open `notationMath.ts` and inspect the Pass 3 gap calculation.
    2. Ensure that `NOTEHEAD_WIDTH` and `ACCIDENTAL_WIDTH` are resolving to actual numbers (e.g., relative `em` values or diatonic steps), NOT evaluating to `undefined` or `NaN`.
    3. Inspect how `leftStack` and `rightStack` are merged and returned to the caller. Ensure the `xOffset` mutations persist in the returned array.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the `C Chromatic5` chord, When the layout pipeline completes, Assert that `shiftX` is a valid number $> 0$, and Assert that the D natural object in the returned array has an `xOffset` exactly equal to its Pass 2 zipper offset PLUS `shiftX`].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute this strict numeric validation test. Do not proceed until you prove the numbers are $> 0$ and surviving the return statement. Once PASS, set status to ✅ Completed.

### Phase 2: Render Layer Object Binding — 🔘 Pending
* **Objective:** Ensure the React/imperative rendering layer is explicitly consuming the new `xOffset` for both the notehead AND the accidental.
* **Instructions:**
    1. Open `NotationCanvas.tsx` (or the equivalent rendering bridge).
    2. Locate the precise line of code where the SMuFL accidental glyph (e.g., `<text>` or `canvas.fillText`) is positioned.
    3. Modify the positioning logic to strictly anchor to `note.xOffset`. Formula: `Accidental_X = note.xOffset - ACCIDENTAL_WIDTH + COMPACTION_OFFSET`.
    4. Ensure the notehead itself is also explicitly applying `note.xOffset`.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a mocked `Note` object with `xOffset: 50` and an attached natural sign, When passed to the rendering component, Assert the React Testing Library DOM output shows the notehead translated by `50` and the accidental translated by `50 - width + compaction`].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Validate the rendering component's data consumption. Once PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 3: Left-Sweep Re-integration — 🔘 Pending
* **Objective:** Ensure the restored accidental left-sweep collision logic respects the new dynamic anchors.
* **Instructions:**
    1. Pass the exact rendered coordinates from Phase 2 into the left-sweep collision engine.
    2. The left-sweep must check for accidental-on-accidental collisions *within* the right stack, but because the entire stack was shifted by `shiftX`, the swept accidentals will naturally fall into the safe gap.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the `C Chromatic5` cluster, When the left-sweep completes, Assert the bounding box of the Right Stack's accidentals does not mathematically intersect with the bounding box of the Left Stack's noteheads].
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Run the collision bounds tests. Once PASS, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify no rogue `NaN` propagations exist in the math utility. Ensure the imperative engine bridge (which bypasses React state for MIDI) is correctly pushing the newly calculated array to the DOM.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:** STOP. Present the final pipeline logic. Instruct the user to play the `C Chromatic5` cluster. Ask the user to visually confirm that the math has successfully reached the screen: the right stack is physically shifted to the right, and its accidentals reside in the gap. Await user confirmation before marking the top-level PRP status as ✅ Completed.