# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
We are performing a final UI calibration on the Transformations Drawer. Currently, the drawer is tucked too far up underneath the main keyboard card when closed, obscuring the chevron tab. We need to adjust the Tailwind translation and margin classes so the tab rests perfectly visible at the bottom edge.

## 2. Implementation Phases

### Phase 1: Drawer CSS Calibration — ✅ Completed
* **Objective:** Adjust the negative margin and Y-translation of the drawer container to expose the chevron tab.
* **Instructions:**
  1. Open the file where the Drawer container is rendered (likely `src/components/toolbar/TransformationsContainer.tsx` or `src/App.tsx`).
  2. Locate the wrapper `div` controlling the open/closed transition. It currently looks something like this:
     `className="... ${isDrawerOpen ? '-mt-10 translate-y-0' : '-mt-10 -translate-y-[130px]'}"`
  3. Reduce the aggressive negative margin and adjust the closed-state translation. Update the class string to this:
     ```tsx
     className={`w-full transition-all duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] z-10 flex flex-col items-center ${isDrawerOpen ? '-mt-4 translate-y-0' : '-mt-4 -translate-y-[105px]'}`}
     ```
  4. *Note to Agent:* If `-translate-y-[105px]` still hides the tab, reduce it to `-translate-y-[95px]`. If it shows too much of the white toolbar, increase it to `-translate-y-[115px]`.
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Apply the CSS adjustment. Set status to ✅ Completed.

## 3. Final Review
* **Status Reconciliation:** Verify Phase 1 is marked ✅ Completed.
* **AGENT INSTRUCTION:** Ask the user to visually confirm if the chevron tab is now perfectly aligned, and instruct them that they can manually tweak the `-105px` value in the file by a few pixels if it needs a micro-adjustment.