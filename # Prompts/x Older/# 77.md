# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
During the previous syntax fixes, the outermost DOM structure of the `NotationCanvas` component was corrupted. The canvas has lost its document flow and positioning context, causing it to render at the top-left of the viewport overlapping the app header. This loss of relative positioning has also completely desynchronized the hit-testing coordinate math. The objective of this micro-sprint is strictly to restore the CSS layout wrappers of the `NotationCanvas`.

## 2. Technical Decisions & Dependencies
* **Positioning Context:** The outermost wrapper of the `NotationCanvas` MUST possess the `relative` CSS class (or `position: relative` in inline styles). This is non-negotiable, as the Glass Panel, Marquee, and note coordinates depend on it.
* **Flex/Grid Flow:** The wrapper must expand to fill its designated area below the header, typically using classes like `flex-1`, `w-full`, `h-full`, and `overflow-hidden`.
* **Zero Logic Modifications:** You are strictly forbidden from modifying state, refs, keyboard event handlers, hit-testing math, or transposition logic.

## 3. Implementation Phases

### Phase 1: Restore Layout Hierarchy — 🔘 Pending
* **Objective:** Fix the JSX wrapper of `NotationCanvas` so it respects the application layout and provides a relative anchor.
* **Instructions:**
    1. Open `src/components/NotationCanvas.tsx`.
    2. Inspect the outermost `return (...)` statement.
    3. Ensure the absolute top-level element is a `<div>` that contains the following Tailwind classes (or equivalent CSS): `relative w-full h-full flex-1`.
    4. If the canvas is intended to be centered, ensure there is a centering wrapper inside it (e.g., `flex items-center justify-center`).
    5. Verify that the "Glass Panel" (`<div className="absolute inset-0 z-50 pointer-events-auto touch-none" ...>`) and the "Marquee Div" (`<div ref={marqueeRef} className="absolute ... z-40">`) are direct children of this `relative` parent, so they correctly overlay the canvas area and do not fly up to the document body.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit the JSX structure and restore the `relative` wrapper. Once completed, set status to ✅ Completed. 

## 4. Final Review & Validation
* **Technical Audit:** Verify you did not alter any functions outside of the `return` statement's JSX class names. 
* **Status Reconciliation:** Verify Phase 1 is ✅ Completed.
* **Final Human Sign-off:**
    * **HUMAN VALIDATION REQUIRED:** "STOP. I have restored the layout wrappers. Please run `npm run dev`. Look at the UI: the grand staff should no longer overlap the header logo and dropdowns. Once it is physically back in place, test the click selection and marquee drag to confirm the coordinate math has resynchronized."