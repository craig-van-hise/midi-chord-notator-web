# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
We have identified an unhandled `Tone.js` exception (`No available buffers for note...`) that is crashing the `handleMidiMessage` thread mid-execution. This thread abortion is causing visual toggles to fail and preventing `NoteOff` events from cleaning up arrays. 
We will:
1. Mask audio engine calls with `try/catch` to prevent execution abortion.
2. Implement a consolidated, foolproof 4-way boolean matrix for the NoteOn/NoteOff state machine.
3. Inject the auto-select pre-flight check into the UI Play button logic.

## 2. Implementation Phases

### Phase 1: The Unbreakable State Machine (Audio & Logic) — ✅ Completed
* **Objective:** Rewrite `handleMidiMessage` in `NotationCanvas.tsx` to safely handle missing audio buffers and execute the exact matrix of Virtual/Physical x Toggle/Hold.
* **Instructions:**
  1. Open `src/components/NotationCanvas.tsx`. Locate `handleMidiMessage`.
  2. Replace the `isNoteOn` block entirely with this exact logic:
     ```typescript
     if (isNoteOn) {
         let shouldAdd = true;
         
         if (isVirtual) {
             if (isToggleModeActive) {
                 const idx = activeNotes.current.findIndex(n => n.note === note);
                 if (idx !== -1) {
                     activeNotes.current.splice(idx, 1); // Toggle Off
                     shouldAdd = false;
                 }
             }
         } else {
             // Physical Hardware
             if (isHoldModeActive && physicalKeysDown.current.size === 0) {
                 activeNotes.current = []; // Wipe board for new chord
             }
             physicalKeysDown.current.add(note);
         }

         if (shouldAdd && !activeNotes.current.some(n => n.note === note)) {
             activeNotes.current.push({ id: generateId(), note, stepOffset: 0, accidental: null, isTreble: note >= splitPointRef.current, velocity: velocity || 100, channel: 0, status: 0x90, sourceMidi: note });
         }

         try { audioEngine.noteOn(Tone.Frequency(note, "midi").toNote(), velocity / 127); } catch(e) { console.warn("Audio buffer missing for noteOn:", note); }
         updateSpellings();
         updateActiveNotes?.([...activeNotes.current]);
     }
     ```
  3. Replace the `isNoteOff` block entirely with this exact logic:
     ```typescript
     else if (isNoteOff) {
         if (!isVirtual) physicalKeysDown.current.delete(note);
         
         try { audioEngine.releaseNote(Tone.Frequency(note, "midi").toNote()); } catch(e) { console.warn("Audio buffer missing for noteOff:", note); }

         let shouldRemove = false;
         if (isVirtual && !isToggleModeActive) {
             shouldRemove = true; // Virtual Momentary
         } else if (!isVirtual && !isHoldModeActive) {
             shouldRemove = true; // Physical Momentary
         }

         if (shouldRemove) {
             activeNotes.current = activeNotes.current.filter(n => n.note !== note);
         }

         updateSpellings();
         updateActiveNotes?.([...activeNotes.current]);
     }
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Execute changes. Set status to ✅ Completed.

### Phase 2: PLAY Button Auto-Select Pre-Flight — ✅ Completed
* **Objective:** Ensure the `PLAY` button automatically selects all active notes if the current selection is empty.
* **Instructions:**
  1. Open `src/components/NotationCanvas.tsx`.
  2. Locate the event listener that handles the `PLAY` button (likely `APP_PLAY` or however the action is dispatched in `TransformationsDrawer.tsx`).
  3. Inside that handler, inject this pre-flight check at the very top:
     ```typescript
     if (selectedNoteIds.current.size === 0 && activeNotes.current.length > 0) {
         activeNotes.current.forEach(note => selectedNoteIds.current.add(note.id));
         forceUpdate(); // Update UI to show selection
     }
     if (selectedNoteIds.current.size === 0) return; // Exit if nothing to play
     ```
  4. Ensure the rest of the audio playback logic uses `try/catch`:
     ```typescript
     // Example playback loop inside the handler
     strings.forEach(s => {
         try { audioEngine.noteOn(s, vel); } catch(e) {}
     });
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Execute changes. Set status to ✅ Completed.

## 3. Final Review
* **Status Reconciliation:** Verify Phase 1 and 2 are marked ✅ Completed.
* **AGENT INSTRUCTION:** Request the user to manually verify Toggle Mode, Hold Mode, and the Play button.