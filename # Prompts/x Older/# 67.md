# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
**Emergency Revert & Unmasking Operation:** The previous agent execution introduced a fatal silent error in the render loop, causing the canvas to go completely blank. Furthermore, it explicitly corrupted the Vite asset pipeline by inserting `public/` into the CSS font URL, breaking the Bravura font.

You are forbidden from writing new features. You will strictly repair the font path, remove the silent `try/catch` black holes that are hiding the rendering crash, and restore the exact React state required to display the notes.

## 2. Technical Decisions & Dependencies
* **Vite Asset Pipeline:** Vite automatically serves the `public/` directory at the root path (`/`). You must NEVER use the word `public` in a CSS `url()` path.
* **Error Unmasking:** Empty `catch (e) {}` blocks are strictly forbidden. If the layout engine crashes, it must log the exact stack trace to the console so the developer can diagnose the coordinate math failure.

## 3. Implementation Phases

### Phase 1: Vite Font Path Correction — 🔘 Pending
* **Objective:** Fix the `invalid sfntVersion` error by aligning the CSS URL with Vite's dev server routing.
* **Instructions:**
    1. Open `src/index.css`.
    2. Locate the `@font-face` declaration for `'Bravura'`.
    3. Change the `src` property EXACTLY to: `src: url('/fonts/Bravura.woff2') format('woff2');`. 
    4. Ensure there is NO `..` and NO `public/` in this URL. Vite will automatically prepend the `base` path during build.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the exact string replacement. Set status to ✅ Completed.

### Phase 2: Destroy Silent Errors & Restore Render State — 🔘 Pending
* **Objective:** Prevent `recalculateLayout` from swallowing fatal math errors, which causes the blank canvas.
* **Instructions:**
    1. In `src/components/NotationCanvas.tsx`, locate the `recalculateLayout` function.
    2. Inside its `catch (e)` block, remove the empty comment. Insert: `console.error("[Layout Engine Crash]:", e);`.
    3. IMMEDIATELY below that console error (still inside the catch block), add a fallback state so the array isn't permanently deadlocked: 
       `setRenderedNotes([]); renderedNotesRef.current = [];`
    4. Locate `updateSpellings()`. Verify it has the `catch (e) { console.error("[Spelling Engine Crash]:", e); }` and ensure `recalculateLayout();` is strictly called in a `finally` block attached to it.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Destroy the silent catch blocks and ensure `console.error` is present. Set status to ✅ Completed.

### Phase 3: JSX DOM Restoration Check — 🔘 Pending
* **Objective:** Ensure the absolute coordinate math didn't wipe out the actual React DOM elements.
* **Instructions:**
    1. In `NotationCanvas.tsx`, scroll down to the `return` statement.
    2. Verify the `#notes-layer` div exists.
    3. Verify that `{renderedNotes.map(note => (...))}` is actively mapping the state array into DOM elements.
    4. Ensure the `.notation-note-container` still relies on `left: calc(50% + ${note.xOffset || 0}px)` and `top: calc(50% - ${note.y}px)`. (If `note.y` is evaluating to `NaN`, the browser will refuse to render it). 
    5. Check the `y` calculation in `recalculateLayout`: `y: ((n.stepOffset + shift) * (staffSpace / 2)) + staffSpace...`. If `n.stepOffset` is undefined, `y` becomes `NaN`. Ensure `n.stepOffset || 0` is used as a fallback to guarantee a valid number.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit the JSX and fallback math. Set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify `public/` is completely gone from `index.css`. Verify `console.error` exists in `recalculateLayout`.
* **Status Reconciliation:** Verify all sub-phases are ✅.
* **Final Human Sign-off:** STOP. Present the build to the user. Ask the user to check their browser console. If the canvas is still blank, the `[Layout Engine Crash]` error will now be visible in the dev tools, and the user can provide it to diagnose the exact line of math that is failing.