# Product Requirements Prompt (PRP) — Remediation Patch 🔘

## 1. Project Context & Objectives
**Current State:** The agent successfully completed the navigation UI cleanup (Undo/Redo removal), the Home button anchor logic, and the core playback routing. However, an audit revealed a failure to implement the physical `VELOCITY` slider UI under the "LISTEN" toggle in Phase 1.
**Objective:** Force immediate remediation of the missing `VELOCITY` parameter UI component in the settings module.

## 2. Implementation Phases

### Phase 1.1: Velocity UI Remediation — ✅ Completed
* **Objective:** Implement the missing Velocity slider directly beneath the "LISTEN" toggle.
* **Instructions:**
  1. Open `src/components/SettingsModal.tsx` (or the exact file housing the "LISTEN" toggle).
  2. Ensure the `usePersistentState` hook (or relevant context) is actively pulling the `uiVelocity` state and its setter.
  3. Inject a new UI block directly below the "LISTEN" toggle:
     * Add a text label/header: "VELOCITY" or "UI Velocity".
     * Add an `<input type="range" min="1" max="127" step="1" />`.
     * Bind the `value` and `onChange` to the `uiVelocity` state.
     * Apply Tailwind 4.1 utility classes to align it seamlessly with the existing modal design system.
* **TDD Checkpoint:**
    * **Test Case 1 (Velocity Render & State):** Mount the Settings modal. Assert that the "VELOCITY" slider exists in the DOM. Fire a change event to adjust the slider value to 100, and assert the persistent state updates correctly.
    * **AGENT INSTRUCTION:** Write and execute this test. Once it PASSES, verify the UI visually via the DOM structure, and set status to ✅ Completed. Do not proceed until status is ✅.
    * **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to open the popup menu and visually confirm the VELOCITY slider is present, functional, and correctly alters the Tone.js transformation playback volume when a UI button is clicked.

## 3. Final Review & Validation
* **Status Reconciliation:** Verify Phase 1.1 is ✅ Completed. Update the master project state to reflect the fully completed UI.