Some TODOs:

- Make the MIDI settings persistent: the app should use the most recent user input/outputs if they are available. 
- Add a strip (rectangle about 15px high) above the keyboard which prints the spelled notes: use Google Jost for the font
- Move the toggle mode button up slightly (to make room for the strip)
- Make the "on" back key highlight lengthen the highlighted area so that it goes all the way to the end (bottom) of the key (-1px for the black outline)