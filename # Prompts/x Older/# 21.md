# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The previous iteration over-corrected the Ottava label padding. The `8va` label is now intersecting the ledger lines of the notehead, and the `8vb` label remains misaligned. The user has explicitly mandated that BOTH labels move UP the screen by exactly 1 Note Space Unit (NSU). 

In web coordinate space (where Y=0 is the top), moving an element "UP" requires subtracting from its `top` position.

## 2. Technical Decisions & Dependencies
* **Target Files:** `src/components/NotationCanvas.tsx`.
* **Metric Standard:** 1 NSU = `var(--staff-space)`.
* **Mathematical Adjustment:**
  * **Treble Label (`8va` / `15ma`):** Currently offset by `-1.8 * var(--staff-space)`. To move it UP by 1 NSU, we must subtract another 1, resulting in `-2.8 * var(--staff-space)`.
  * **Bass Label (`8vb` / `15mb`):** Currently offset by `+1.8 * var(--staff-space)`. To move it UP by 1 NSU, we must subtract 1, resulting in `+0.8 * var(--staff-space)`.

## 3. Implementation Phases

### Phase 1: Explicit Y-Axis Offset Correction — ✅ Completed
* **Objective:** Apply the exact `1.0 NSU` upward shift to both transposition labels.
* **Instructions (Target: `src/components/NotationCanvas.tsx` -> `renderActiveNotes`):**
    1. Locate the `trebleLabel` DOM creation logic.
    2. Change the inline style offset from `1.8` to `2.8`:
       `labelEl.style.top = calc(${highestNoteTop} - var(--staff-space) * 2.8);`
    3. Locate the `bassLabel` DOM creation logic.
    4. Change the inline style offset from `1.8` to `0.8`:
       `labelEl.style.top = calc(${lowestNoteTop} + var(--staff-space) * 0.8);`
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a Treble shift, Assert `trebleLabel` `top` style explicitly subtracts `2.8` staff spaces]
    * **Test Case 2:** [Given a Bass shift, Assert `bassLabel` `top` style explicitly adds `0.8` staff spaces]
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute changes. Once PASS, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Double-check that no other padding, margin, or transform rules were added that would conflict with this absolute `top` value.
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed.
* **Final Human Sign-off:**
    * **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to:
        1. Play C7. Verify the `8va` label has moved up and no longer intersects the notehead's ledger lines.
        2. Play A0. Verify the `8vb` label has moved up closer to the notehead."