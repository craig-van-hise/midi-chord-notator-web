# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
During the recent data restoration of `PCS_LUT.json`, it was discovered that `Python_Port/dictionaries.py` still contains the erroneous manual overrides that caused the data corruption in the first place. 

**Objective:** Perform a surgical cleanup of `dictionaries.py` to remove the bad overrides without losing the recent, successful feature additions.

## 2. Technical Decisions & Dependencies
* **Surgical Deletion:** We cannot use `git restore` on `dictionaries.py` because it contains recent code we want to keep. The agent must use standard file editing to remove *only* the specific offending lines.
* **Target Data:** The specific overrides to hunt down are those forcibly assigning `m9(13,no5)` (or similar incorrect mode functions/chord types) to the rotational family of 1549. These indices are: `107`, `429`, `1131`, `1411`, `1443`, and `1677` (and potentially `1549` itself if it was incorrectly formatted as part of a batch override).

## 3. Implementation Phases

### Phase 1: Surgical Dictionary Cleanup — 🔘 Pending
* **Objective:** Remove the corrupted hardcoded overrides from the Python generator.
* **Instructions:**
  1. Open `Python_Port/dictionaries.py`.
  2. Search for the dictionary entries/overrides targeting indices `107`, `429`, `1131`, `1411`, `1443`, and `1677`.
  3. Delete *only* the lines or blocks applying the `m9(13,no5)` data to these indices.
  4. Ensure no syntax errors (like trailing commas or unclosed brackets) are left behind in the Python dictionary structure.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the cleaned `dictionaries.py`, When `python3 lut_main.py` is executed, Assert the script runs to completion without throwing syntax or key errors].
  * **Test Case 2:** [Given the newly generated JSON, When inspecting index `107`, Assert the 'chord_type' is `" Locrian5"` and NOT `"m9(13,no5)"`].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the targeted deletion, run the Python generation script, and verify the output JSON. Once verified, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Run a `git diff Python_Port/dictionaries.py` and verify that *only* the bad override lines were removed, and no other recent logic was touched.
* **Status Reconciliation:** Verify Phase 1 is marked `✅ Completed` before marking the top-level PRP status as `✅ Completed`.