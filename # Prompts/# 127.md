# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The project is currently failing its production build (CI/CD) due to strict TypeScript compiler errors (`tsc`). The local development environment (Vite/esbuild) masked these errors because it bypasses type-checking. The objective is to systematically resolve all type errors, missing module references, null-safety violations, and unused variable declarations to achieve a clean compilation and successful web deployment.

## 2. Technical Decisions & Dependencies
* **Tone.js Sampler Limits:** The `Sampler` class in Tone.js does not expose `decay` or `sustain` properties directly on the instance (it only accepts `attack` and `release`). You must remove the invalid assignments to `decay` and `sustain` in `engine.ts`.
* **TypeScript Verbatim Module Syntax:** The `tsconfig.json` enforces `verbatimModuleSyntax`. Any import that is used exclusively for types *must* be prefixed with `type` (e.g., `import type { ButtonId } from...`).
* **React 17+ JSX Transform:** The project uses Vite with React 19. The `import React from 'react';` statement is entirely obsolete for JSX files and triggers unused variable errors. They must be removed.
* **Module Resolution Check:** The `NavController.tsx` is looking for `../types`. Based on the project tree, the correct file is `navTypes.ts` located in the same directory. 

## 3. Implementation Phases

### Phase 1: Imports, Module Resolution & Unused Locals — ✅ Completed
* **Objective:** Clean up the lowest-hanging fruit: unused variables, legacy React imports, and broken paths.
* **Instructions:**
    1. **`src/components/NavController.tsx`**: Update the import path. Change `from '../types'` to `from './navTypes'`.
    2. **`src/components/VUMeter.tsx`**: Remove `import React from 'react';`.
    3. **`src/components/NotationCanvas.events.test.tsx`**: Remove unused imports/declarations for `fireEvent`, `Tone`, and `svg`.
    4. **`src/midi/MIDIProvider.tsx`**: Remove the unused `heldHardwareNotes` variable, or if it is required for an upcoming feature, prefix it with an underscore (`_heldHardwareNotes`) to bypass the linter.
    5. **`src/utils/notationMath.ts`**: Remove or underscore-prefix the unused `pc` variable on line 347.
    6. **`src/components/toolbar/ToolbarContextMenus.tsx`**: Update the import to use the `type` keyword: `import type { ButtonConfig, ButtonId } ...`.
    7. **`src/components/toolbar/TransformationsToolbar.test.tsx`**: Update type imports with the `type` keyword and remove `import React from 'react';`.
* **TDD Checkpoint:** * **Test Action:** Run `npx tsc --noEmit`.
    * **Expected Outcome:** The specific TS6133, TS2307, and TS1484 errors listed above must no longer appear in the console output.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Make the code changes and execute the validation. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Null Safety & Test Object Shapes — ✅ Completed
* **Objective:** Satisfy strict null checks and correct missing properties on test mocks.
* **Instructions:**
    1. **`src/utils/chordSpeller.ts`**: Lines 167 and 184 indicate `entry` might be null. Add an explicit type guard before these lines. For example: `if (!entry) { console.warn("PCS entry not found"); return null; }` (adapt the return type to match the function signature gracefully).
    2. **`src/components/toolbar/TransformationsToolbar.test.tsx`**: The mocks for `Record<ButtonId, boolean>` and `ButtonConfigMap` are failing because they are missing the `KEY_UP` and `KEY_DOWN` properties. Add these properties to the mock objects in the test suite (lines 28, 29, 46, 47, 69, 70).
* **TDD Checkpoint:**
    * **Test Action:** Run `npx tsc --noEmit` AND `npm run test run`.
    * **Expected Outcome:** TS18047 and TS2739 errors are resolved. The Vitest suite must continue to pass.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute these tests. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 3: Audio Engine Instantiation & Interfaces — ✅ Completed
* **Objective:** Fix the critical initializers and invalid assignments in the Tone.js audio engine.
* **Instructions:**
    1. **`src/audio/engine.ts` (Line 15):** Fix the uninitialized `buffers` property. Either initialize it in the constructor or use the definite assignment assertion if it is guaranteed to be hydrated later: `buffers!: Tone.ToneAudioBuffers;`.
    2. **`src/audio/engine.ts` (Lines 325, 334):** The code attempts to assign `.decay` and `.sustain` directly to a Tone.js `Sampler` instance. Remove these assignments completely. If envelope control is strictly required for the sampler, you must pass `attack` and `release` into the `Sampler` constructor options instead.
* **TDD Checkpoint:**
    * **Test Action:** Run `npx tsc --noEmit`.
    * **Expected Outcome:** TS2564 and TS2339 errors in `engine.ts` are resolved.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Make the code changes and execute the validation. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅.

## 4. Final Review & Validation
* **Technical Audit:** Execute `npm run build`. The build command runs `tsc -b && vite build`. 
* **Status Reconciliation:** Verify all sub-phases are marked ✅ Completed. Only then may you mark the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** STOP. Output a message informing the user that the build process has succeeded. Request that the user run `npm run build` locally to verify the production bundle is generated without errors before committing the fix.