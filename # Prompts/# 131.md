
**Context:** The previous download failed because the Steinberg repository does not host the `.woff2` file at that specific raw path. We are now going to use the official, verified VexFlow CDN link for the Bravura `.woff2` file.

**Instructions:**

1. Run the following exact command to download the raw binary `.woff2` file from the VexFlow CDN:
`curl -sL "https://cdn.jsdelivr.net/npm/@vexflow-fonts/bravura/bravura.woff2" -o public/fonts/Bravura.woff2`
2. Verify that the downloaded file is a legitimate binary font by running:
`file public/fonts/Bravura.woff2 && ls -lh public/fonts/Bravura.woff2`
3. **Validation Check:** - If the file is tiny (under 10KB) or says "ASCII text", delete it and tell me it failed.

* The correct file should be roughly **120KB to 130KB** and the `file` command should identify it as a `Web Open Font Format` data file.

4. Once the binary is verified, stage the file, commit it with the message "Fix Bravura font binary (finally)", and push it to the remote repository so GitHub Pages can deploy it. **Do not modify any other code.**