# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The previous CSS adjustment was mathematically incorrect. By altering both the top margin and the Y-translation simultaneously, the drawer was pushed down by ~50px, exposing the entire bottom row of buttons in the closed state. We must revert the margin and apply a precise 15px adjustment exclusively to the Y-translation to reveal only the chevron tab.

## 2. Implementation Phases

### Phase 1: Precision CSS Calibration — ✅ Completed
* **Objective:** Fix the botched Drawer closed-state coordinates.
* **Instructions:**
  1. Open the file rendering the drawer wrapper (likely `src/components/toolbar/TransformationsContainer.tsx` or `src/App.tsx`).
  2. Locate the wrapper `div` controlling the open/closed transition.
  3. Replace the template literal `className` string with this exact calculation:
     ```tsx
     className={`w-full transition-all duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] z-10 flex flex-col items-center ${isDrawerOpen ? '-mt-10 translate-y-0' : '-mt-10 -translate-y-[115px]'}`}
     ```
* **AGENT INSTRUCTION:** Set status to 🔵 Active. Apply this exact class string. Set status to ✅ Completed.

## 3. Final Review
* **Status Reconciliation:** Verify Phase 1 is marked ✅ Completed.