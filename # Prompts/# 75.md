# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
This is a hyper-focused sprint to implement the Visual Marquee drag-box and fix the "Unison Erasure" bug during transposition. Previous attempts failed because the worker agent went rogue, used destructive bash commands (`sed`), and modified core layout math. 

**STRICT GUARDRAILS:**
* **NO `sed` OR BASH SCRIPTING:** You are explicitly forbidden from using `sed`, `awk`, or renaming files via the terminal. 
* **NO LAYOUT MODIFICATIONS:** You are strictly forbidden from modifying `recalculateLayout`, `updateSpellings`, or any of the mathematical logic used to position the notes or ledger lines.
* **NO ASSET MODIFICATIONS:** Do not touch CSS URLs, Vite configs, or font files. 

## 2. Technical Decisions & Dependencies
* **Unison Tracking:** We must track note instances by their array index, NOT just their raw MIDI pitch. The selection state must be updated to store string composites: `"${midiNote}-${index}"`.
* **Visual Marquee:** Use a dedicated `<div ref={marqueeRef}>` controlled purely via imperative inline styles in the `onPointerMove` handler.

## 3. Implementation Phases

### Phase 1: Implement the Visual Marquee UI — 🔘 Pending
* **Objective:** Render the blue marquee drag-box without triggering React re-renders.
* **Instructions:**
    1. In `NotationCanvas.tsx`, add a new ref: `const marqueeRef = useRef<HTMLDivElement>(null);`
    2. In your JSX, immediately preceding the "Glass Panel" `<div>`, add: 
       `<div ref={marqueeRef} className="absolute border border-blue-500 bg-blue-500/20 z-40 pointer-events-none hidden" style={{ left: 0, top: 0, width: 0, height: 0 }} />`
    3. In the Glass Panel's `onPointerDown`, if starting a drag, store the `startX` and `startY` (using `e.nativeEvent.offsetX` and `offsetY`).
    4. In `onPointerMove` (if dragging):
       * Calculate `left = Math.min(startX, currentX)`
       * Calculate `top = Math.min(startY, currentY)`
       * Calculate `width = Math.abs(currentX - startX)`
       * Calculate `height = Math.abs(currentY - startY)`
       * Apply to `marqueeRef.current.style`.
       * Remove the `hidden` class: `marqueeRef.current.classList.remove('hidden')`.
    5. In `onPointerUp`: Add the `hidden` class back.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Carefully implement exactly as written. Once complete, set status to ✅ Completed. Do NOT write extensive unit tests that mock the DOM for this specific UI feature.

### Phase 2: Fix Unison Erasure (ID/Index Refactoring) — 🔘 Pending
* **Objective:** Allow duplicate MIDI pitches to be selected and transposed independently.
* **Instructions:**
    1. Update the `selectedNoteIds` ref definition (if typed) to clearly accept string composites: `useRef<Set<string>>(new Set())`.
    2. **Hit-Testing Update:** When selecting a note via click or marquee, store its ID as `"${note.midiPitch}-${index}"` (where `index` is its position in the `activeNotes` array).
    3. **Rendering Update:** When applying the visual "selected" style to the SMuFL notes, check `selectedNoteIds.current.has(`${note.midiPitch}-${index}`)`.
    4. **Transposition Update:** In the keyboard shortcut handlers (Shift+Up/Down, etc.):
        * Map over `activeNotes` utilizing the `index`. 
        * Check if `"${note.midiPitch}-${index}"` is in the `selectedNoteIds`. If yes, apply the transposition math to *that specific note only*.
        * Pass the resulting array directly to `updateActiveNotes()`. **Remove any `new Set( ... )` deduplication** from this pipeline.
        * *Crucial Step:* Rebuild the `selectedNoteIds.current` Set with the newly transposed values `"${newMidiPitch}-${index}"` so the note remains selected after it moves.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Implement the refactoring carefully. Once complete, set status to ✅ Completed. 

## 4. Final Review & Validation
* **Technical Audit:** Verify `sed` was never used. Verify the core layout math (`notationMath.ts` and canvas layout functions) remains 100% untouched.
* **Status Reconciliation:** Verify Phase 1 and 2 are ✅ Completed.
* **Final Human Sign-off:**
    * **HUMAN VALIDATION REQUIRED:** "STOP. I have implemented the Marquee div and refactored the selection IDs. Please test the Marquee box to see if it visually draws. Then, test transposing a note into a unison to verify it does not get erased."