# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The application has been safely rolled back to a stable state: fonts are loading (no 404s), `forceUpdate` crashes are gone, click-selection works, and transpositions work. However, there is no Marquee selection, and transposing a note into an existing note causes "Unison Erasure." 

**ABSOLUTE GUARDRAILS:**
1. **DO NOT DELETE JSX:** You are strictly forbidden from altering, replacing, or deleting the existing `return (...)` structure, the SVG elements, or the mapping loops that render the notation. 
2. **APPEND ONLY:** The visual Marquee must be injected as a single new `<div>` just before the final closing tag of the component.
3. **NO MATH CHANGES:** Do not touch `notationMath.ts` or any Y-coordinate positioning logic.

## 2. Technical Decisions & Dependencies
* **Unison Tracking:** To prevent Unison Erasure, `selectedNoteIds` must track notes by their array index, not just their pitch. We will use a composite string: `"${note.midiPitch}-${index}"`.
* **Marquee Rendering:** The Marquee will be a hidden, absolute-positioned `<div>` controlled purely by a React `useRef` to avoid React render lag. 

## 3. Implementation Phases

### Phase 1: Prevent Unison Erasure (JS Logic Only) — ✅ Completed
* **Objective:** Allow duplicate pitches by tracking selection via array indices.
* **Instructions:**
    1. In `NotationCanvas.tsx`, locate the `selectedNoteIds` ref/state. Ensure it expects strings.
    2. **Hit-Testing Update:** Where the code handles clicking a note, update it so it adds `"${pitch}-${index}"` to the selection set instead of just the pitch.
    3. **Visual Highlight Update:** In the JSX where the CSS classes for selected notes are applied, change the condition to check `selectedNoteIds.current.has(`${note.midiPitch}-${index}`)`.
    4. **Transposition Update:** In the keyboard handlers (Up/Down arrows):
        * Map over the `activeNotes` array by index.
        * If `selectedNoteIds.current.has(`${note.midiPitch}-${index}`)`, apply the transposition to that specific note.
        * Rebuild the `selectedNoteIds` set with the newly transposed values (e.g., `"${newPitch}-${index}"`) so the note stays highlighted after moving.
        * Pass the mapped array to `updateActiveNotes`. **Ensure there are NO `new Set()` calls that would remove duplicates.**
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Apply these logic changes carefully. Do not touch the JSX beyond the selection condition. Once complete, set status to ✅ Completed.

### Phase 2: Inject the Marquee UI (Append Only) — ✅ Completed
* **Objective:** Add the drag-box UI without breaking the existing DOM tree.
* **Instructions:**
    1. Add `const marqueeRef = useRef<HTMLDivElement>(null);` to the component.
    2. Add tracking variables (via `useRef` or outside the render cycle) for `startX` and `startY`.
    3. Locate the absolute final closing `</div>` or `</svg>` of the `NotationCanvas` return statement.
    4. **APPEND** this exact line right before the closing tag: 
       `<div ref={marqueeRef} className="absolute border border-blue-500 bg-blue-500/20 z-50 pointer-events-none hidden" style={{ left: 0, top: 0, width: 0, height: 0 }} />`
    5. In the existing `onPointerDown` (or `onMouseDown`) handler attached to the canvas container, record `startX` and `startY` using `e.nativeEvent.offsetX` and `offsetY`.
    6. In `onPointerMove`: Calculate `left`, `top`, `width`, and `height` based on the delta from start coordinates. Apply them directly to `marqueeRef.current.style` and call `marqueeRef.current.classList.remove('hidden')`.
    7. In `onPointerUp`: Call `marqueeRef.current.classList.add('hidden')` and implement the range intersection math to update `selectedNoteIds`.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Implement the Marquee logic and append the `div`. Once complete, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify that you did NOT delete any existing layout code, SVGs, or mapping functions. The app must still render the exact same visual notation as before.
* **Status Reconciliation:** Verify Phase 1 and 2 are ✅ Completed.
* **Final Human Sign-off:**
    * **HUMAN VALIDATION REQUIRED:** "STOP. I have updated the selection logic for unisons and appended the Marquee box. Please run the app. First, verify the notation is still visually on the screen. Then, test the Marquee drag, and test transposing a note into a unison to ensure it doesn't disappear."