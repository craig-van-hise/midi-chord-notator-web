# Product Requirements Prompt (PRP) — Remediation Patch 🔘

## 1. Project Context & Objectives
**Project:** Grand Staff MIDI Notator
**Current State:** The `MidiNoteRangeFilter` UI successfully renders in its modal, and the `applyNoteFilter` logic exists. However, the agent completely failed to wire the mathematical filter into the active audio pipeline, and the visual feedback on the modal's 128-key piano is dead.
**Objective:** Hardwire the filter into the main audio output pipeline (between the transformation engine and Tone.js) and hook up the imperative `updateKeyVisuals128` function so the on-screen filter keyboard lights up dynamically when notes are playing.

## 2. Technical Decisions & Dependencies
* **Interception Point:** The routing *must* happen at the exact moment a transformed chord is dispatched to `audioEngine.triggerAttack()`. This is likely housed in `src/midi/MIDIProvider.tsx`.
* **State Access:** `MIDIProvider.tsx` must explicitly consume the `filterMode` and `filterRange` variables from `usePersistentState.ts`.
* **Visual imperative bridge:** The `MidiNoteRangeFilter.tsx` exports `updateKeyVisuals128(note: number, color: string)`. You must import this function into `MIDIProvider.tsx` (or the audio event loop) and invoke it iteratively for every note in the filtered chord upon Note On/Note Off.

## 3. Implementation Phases

### Phase 1: Hardwiring Audio Interception (The "Rompler" Gate) — ✅ Completed
* **Objective:** Force the active `filterMode` and `filterRange` to mutate the outgoing MIDI note array before it hits Tone.js.
* **Instructions:**
  1. Open `src/midi/MIDIProvider.tsx` (or the file responsible for executing transformation triggers and calling `audioEngine`).
  2. Ensure the hook `usePersistentState` is imported and extracting `filterMode` and `filterRange`.
  3. Locate the specific block where a transformation is calculated (e.g., `const transformedNotes = applyTransformation(...)`).
  4. Immediately following the transformation, apply the filter:
     ```typescript
     import { applyNoteFilter } from '../lib/midiProcessing';
     // ... inside the event handler
     const filteredNotes = applyNoteFilter(transformedNotes, filterMode, filterRange);
     ```
  5. **CRITICAL:** Update the Map/Ref tracking the active notes. You MUST map the incoming hardware trigger note to the `filteredNotes`, NOT the `transformedNotes`. 
     ```typescript
     activeTransformationNotesRef.current.set(triggerNote, filteredNotes);
     ```
  6. Execute `audioEngine.triggerAttack(filteredNotes, velocity)` ONLY if `filteredNotes.length > 0`.
* **TDD Checkpoint:**
    * **Test Case 1 (Audio Interception via State):** Given `filterMode = 'block'` and `filterRange = [60, 72]`. When a transformation outputs `[50, 64, 80]`, Then `audioEngine.triggerAttack` is called ONLY with `[64]`.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute this test. Once it PASSES, set status to ✅ Completed.

### Phase 2: Wiring Visual Feedback (`updateKeyVisuals128`) — ✅ Completed
* **Objective:** Light up the keys on the filter modal's 128-key piano to show the user exactly which notes are surviving the filter and being played.
* **Instructions:**
  1. Import `updateKeyVisuals128` from `src/components/MidiNoteRangeFilter.tsx` into `MIDIProvider.tsx` (or your central audio dispatch file).
  2. **Note On:** Right after calling `audioEngine.triggerAttack(filteredNotes, velocity)`, iterate over `filteredNotes` and call:
     ```typescript
     filteredNotes.forEach(note => updateKeyVisuals128(note, '#3b82f6')); // Blue for active
     ```
  3. **Note Off:** Right after calling `audioEngine.triggerRelease(notesToRelease)`, iterate over those notes and turn them off:
     ```typescript
     notesToRelease.forEach(note => updateKeyVisuals128(note, '')); // Empty string clears color
     ```
* **TDD Checkpoint:**
    * **Test Case 1 (Visual Bridge Invocation):** Given a filtered output of `[60, 64]`, When the Note On logic executes, Then the spy for `updateKeyVisuals128` is called twice with `(60, '#3b82f6')` and `(64, '#3b82f6')`.
    * **Mocking Requirements:** Spy on the `updateKeyVisuals128` export.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute this test. Once it PASSES, set status to ✅ Completed.
    * **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to:
      1. Open the Filter Modal.
      2. Set a restriction (e.g., Block, range [60, 72]).
      3. Play a chord on their MIDI controller that triggers a transformation outside this range.
      4. Visually confirm the modal's piano lights up blue ONLY for the notes that actually play (the filtered notes), and that the audio accurately reflects this.

## 4. Final Review & Validation
* **Technical Audit:** Ensure no orphaned variables exist. Verify that `activeTransformationNotesRef` accurately maps the released notes to avoid Tone.js hanging buffer issues.
* **Status Reconciliation:** Verify Phase 1 and Phase 2 are ✅ Completed. Do not mark the top-level status as Complete until manual user validation confirms audio and visual feedback are fully operational.